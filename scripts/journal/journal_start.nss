void main()
{
    object oPC = getEnteringObject();
    AddJournalQuestEntry("Main", 0001, oPC);

    //AddJournalQuestEntry("Tag of Quest", 60 [it's number ID on the entry], oPC [player character object, declared before this runs]);
}